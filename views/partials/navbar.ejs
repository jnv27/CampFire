<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">CampFire</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup"
            aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <a class="nav-link" href="/">Home</a>
                <a class="nav-link" href="/campgrounds">Campgrounds</a>
                <a class="nav-link" href="/campgrounds/new">New Campground</a>
            </div>
            <div class="navbar-nav ml-auto">
                <% if(!currentUser) {%>
                    <a class="nav-link" href="/login"><i class="fas fa-sign-in-alt"></i> Sign In</a>
                    <a class="nav-link" href="/register"><i class="fas fa-user-plus"></i> Register</a>
                <% } else {%>
                    <a class="nav-link" href="#"><i class="fas fa-user"></i> <%= currentUser.username[0].toUpperCase() + currentUser.username.substring(1) %></a>
                    <a class="nav-link" href="/logout"><i class="fas fa-sign-out-alt"></i> Sign out</a>
                <% } %>
            </div>
        </div>
        
    </div>
</nav> 